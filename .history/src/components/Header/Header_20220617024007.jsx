import React from 'react';
import './Header.style.css';

const Header = () => {
    return (
        <header>
            .container.header__container
        </header>
        
    );
};

export default Header;