import React from 'react';
import './Header.style.css';

const Header = () => {
    return (
        <header>
            .container.header
        </header>
        
    );
};

export default Header;